namespace Cohosts.ConsoleApp
{
    class Commands
    {
        /// <summary>
        /// Command to add new record to the Hosts
        /// </summary>
        internal const string ADD = "add";

        /// <summary>
        /// Command to remove records from the Hosts
        /// </summary>
        internal const string REMOVE = "remove";

        /// <summary>
        /// Command to show existing records in the Hosts
        /// </summary>
        internal const string SHOW = "show";
    }
}
